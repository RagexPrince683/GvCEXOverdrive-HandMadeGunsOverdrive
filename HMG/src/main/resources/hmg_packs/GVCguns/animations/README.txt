Put optional external gun animation JSON files in this directory and reference
them with Animations,akm.json. The Blockbench AK example uses animations embedded
in models/cod4_ak.bbmodel, so it does not require a separate JSON file.
